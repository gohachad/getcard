# getcard.kg — public marketing site. Both language routes are open to every
# crawler, and the sitemap below lists them.
#
# Only the customer's own paths stay out: tracking URLs carry an order code in
# the query string, and /api/ is not a page at all. Those run on this same host
# because a client should not change domain mid-purchase — the restriction is
# about privacy, not about keeping the site out of search.

User-agent: *
Allow: /
Disallow: /track
Disallow: /api/
Disallow: /app-assets/

Sitemap: https://getcard.kg/sitemap.xml
